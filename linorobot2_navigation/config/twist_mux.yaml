twist_mux:
  ros__parameters:
    output: cmd_vel_omni
    topics:
      navigation:
        topic   : /cmd_vel
        timeout : 0.5
        priority: 10

      joystick  :
        topic   : /cmd_vel_joy
        timeout : 0.5
        priority: 100